services:
  nginx:
    image: dan1sh7/niklo-nginx:latest
    depends_on:
      - auth-service
      - bus-service
      - payment-service
      - hotel-service
      - user-service
      - ride-service
      - driver-service
      - package-service
      - adventure-service
      - notification-service
      - admin-service
      - booking-service
    expose:
      - '80'
    healthcheck:
      test:
        - CMD-SHELL
        - wget -q -O /dev/null http://localhost/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    labels:
      - traefik.enable=true
      - >-
        traefik.http.routers.nginx.rule=Host(irtucumwj9akelon0k4iyatv.187.127.157.13.sslip.io)
      - traefik.http.services.nginx.loadbalancer.server.port=80
  auth-service:
    image: dan1sh7/niklo-auth-service:latest
    environment:
      NODE_ENV: development
      PORT: 3001
      DB_HOST: postgres-db
      DB_PORT: 5432
      DB_USERNAME: niklo_auth
      DB_USER: niklo_auth
      DB_PASSWORD: niklo_auth_password
      DB_NAME: niklo_auth
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: ${JWT_SECRET:-change-me-before-deploy}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-15m}
      JWT_REFRESH_EXPIRES_IN: ${JWT_REFRESH_EXPIRES_IN:-7d}
    depends_on:
      - postgres-db
      - redis
    healthcheck:
      test:
        - CMD-SHELL
        - wget -q -O /dev/null http://127.0.0.1:PORT/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
  bus-service:
    image: dan1sh7/niklo-bus-service:latest
    environment:
      NODE_ENV: development
      PORT: 3003
      DB_HOST: postgres-db
      DB_PORT: 5432
      DB_USERNAME: niklo_bus
      DB_USER: niklo_bus
      DB_PASSWORD: niklo_bus_password
      DB_NAME: niklo_bus
      JWT_SECRET: ${JWT_SECRET:-change-me-before-deploy}
    depends_on:
      - postgres-db
    healthcheck:
      test:
        - CMD-SHELL
        - wget -q -O /dev/null http://127.0.0.1:PORT/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
  payment-service:
    image: dan1sh7/niklo-payment-service:latest
    environment:
      NODE_ENV: development
      PORT: 3007
      DB_HOST: postgres-db
      DB_PORT: 5432
      DB_USERNAME: niklo_payment
      DB_USER: niklo_payment
      DB_PASSWORD: niklo_payment_password
      DB_NAME: niklo_payment
      JWT_SECRET: ${JWT_SECRET:-change-me-before-deploy}
      RAZORPAY_KEY_ID: ${RAZORPAY_KEY_ID:-replace-me}
      RAZORPAY_KEY_SECRET: ${RAZORPAY_KEY_SECRET:-replace-me}
      RAZORPAY_WEBHOOK_SECRET: ${RAZORPAY_WEBHOOK_SECRET:-replace-me}
    depends_on:
      - postgres-db
    healthcheck:
      test:
        - CMD-SHELL
        - wget -q -O /dev/null http://127.0.0.1:PORT/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
  hotel-service:
    image: dan1sh7/niklo-hotel-service:latest
    environment:
      NODE_ENV: development
      PORT: 3004
      DB_HOST: postgres-db
      DB_PORT: 5432
      DB_USERNAME: niklo_hotel
      DB_USER: niklo_hotel
      DB_PASSWORD: niklo_hotel_password
      DB_NAME: niklo_hotel
      JWT_SECRET: ${JWT_SECRET:-change-me-before-deploy}
    depends_on:
      - postgres-db
    healthcheck:
      test:
        - CMD-SHELL
        - wget -q -O /dev/null http://127.0.0.1:PORT/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
  user-service:
    image: dan1sh7/niklo-user-service:latest
    environment:
      NODE_ENV: development
      PORT: 3002
      DB_HOST: postgres-db
      DB_PORT: 5432
      DB_USERNAME: niklo_user
      DB_USER: niklo_user
      DB_PASSWORD: niklo_user_password
      DB_NAME: niklo_user
      JWT_SECRET: ${JWT_SECRET:-change-me-before-deploy}
    depends_on:
      - postgres-db
    healthcheck:
      test:
        - CMD-SHELL
        - wget -q -O /dev/null http://127.0.0.1:PORT/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
  ride-service:
    image: dan1sh7/niklo-ride-service:latest
    environment:
      NODE_ENV: development
      PORT: 3005
      DB_HOST: postgres-db
      DB_PORT: 5432
      DB_USERNAME: niklo_ride
      DB_USER: niklo_ride
      DB_PASSWORD: niklo_ride_password
      DB_NAME: niklo_ride
      JWT_SECRET: ${JWT_SECRET:-change-me-before-deploy}
      REDIS_HOST: redis
      REDIS_PORT: 6379
    depends_on:
      - postgres-db
      - redis
    healthcheck:
      test:
        - CMD-SHELL
        - wget -q -O /dev/null http://127.0.0.1:PORT/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
  driver-service:
    image: dan1sh7/niklo-driver-service:latest
    environment:
      NODE_ENV: development
      PORT: 3011
      DB_HOST: postgres-db
      DB_PORT: 5432
      DB_USERNAME: niklo_driver
      DB_USER: niklo_driver
      DB_PASSWORD: niklo_driver_password
      DB_NAME: niklo_driver
      JWT_SECRET: ${JWT_SECRET:-change-me-before-deploy}
    depends_on:
      - postgres-db
    healthcheck:
      test:
        - CMD-SHELL
        - wget -q -O /dev/null http://127.0.0.1:PORT/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
  package-service:
    image: dan1sh7/niklo-package-service:latest
    environment:
      NODE_ENV: development
      PORT: 3012
      DB_HOST: postgres-db
      DB_PORT: 5432
      DB_USERNAME: niklo_package
      DB_USER: niklo_package
      DB_PASSWORD: niklo_package_password
      DB_NAME: niklo_package
      JWT_SECRET: ${JWT_SECRET:-change-me-before-deploy}
    depends_on:
      - postgres-db
    healthcheck:
      test:
        - CMD-SHELL
        - wget -q -O /dev/null http://127.0.0.1:PORT/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
  adventure-service:
    image: dan1sh7/niklo-adventure-service:latest
    environment:
      NODE_ENV: development
      PORT: 3013
      DB_HOST: postgres-db
      DB_PORT: 5432
      DB_USERNAME: niklo_adventure
      DB_USER: niklo_adventure
      DB_PASSWORD: niklo_adventure_password
      DB_NAME: niklo_adventure
      JWT_SECRET: ${JWT_SECRET:-change-me-before-deploy}
    depends_on:
      - postgres-db
    healthcheck:
      test:
        - CMD-SHELL
        - wget -q -O /dev/null http://127.0.0.1:PORT/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
  notification-service:
    image: dan1sh7/niklo-notification-service:latest
    environment:
      NODE_ENV: development
      PORT: 3009
      DB_HOST: postgres-db
      DB_PORT: 5432
      DB_USERNAME: niklo_notification
      DB_USER: niklo_notification
      DB_PASSWORD: niklo_notification_password
      DB_NAME: niklo_notification
      JWT_SECRET: ${JWT_SECRET:-change-me-before-deploy}
    depends_on:
      - postgres-db
    healthcheck:
      test:
        - CMD-SHELL
        - wget -q -O /dev/null http://127.0.0.1:PORT/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
  admin-service:
    image: dan1sh7/niklo-admin-service:latest
    environment:
      NODE_ENV: development
      PORT: 3020
      JWT_SECRET: ${JWT_SECRET:-change-me-before-deploy}
      REDIS_HOST: redis
      REDIS_PORT: 6379
    depends_on:
      - postgres-db
      - redis
    healthcheck:
      test:
        - CMD-SHELL
        - wget -q -O /dev/null http://127.0.0.1:PORT/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
  booking-service:
    image: dan1sh7/niklo-booking-service:latest
    environment:
      NODE_ENV: development
      PORT: 3014
      DB_HOST: postgres-db
      DB_PORT: 5432
      DB_USERNAME: postgres
      DB_USER: postgres
      DB_PASSWORD: niklo_postgres_password
      DB_NAME: postgres
      JWT_SECRET: ${JWT_SECRET:-change-me-before-deploy}
      REDIS_HOST: redis
      REDIS_PORT: 6379
    depends_on:
      - postgres-db
      - redis
    healthcheck:
      test:
        - CMD-SHELL
        - wget -q -O /dev/null http://127.0.0.1:PORT/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
  postgres-db:
    image: dan1sh7/niklo-postgres-db:latest
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: niklo_postgres_password
    volumes:
      - postgres-db-data:/var/lib/postgresql/data
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U postgres -d postgres
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
  redis:
    image: redis:7-alpine
    volumes:
      - redis-data:/data
    healthcheck:
      test:
        - CMD
        - redis-cli
        - ping
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
volumes:
  postgres-db-data: null
  redis-data: null
